import React from 'react';
import MenualModal from './MenualModal';
import styled from 'styled-components';

const MenualButton = () => {
    const [modal, setModal] = React.useState(false);


    return (
        <>
            <p>{'사용방법'}</p>
        </>
    );
}

export default MenualButton;